class Node{
    // This is variable to store data in the list 
    int data;
    // This is the index to maintain index
    int index;
    // This is pointer to next node for list  
    Node next;
    // Constructor to initialize the data field in list
    Node (int data){
        this.data = data;
    }
}
